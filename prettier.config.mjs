export default {
  semi: true,             // Always end lines with a semicolon
  singleQuote: true,      // Use single quotes everywhere
  trailingComma: 'all',   // Enable trailing commas
  printWidth: 100,        // Wrap lines at 100 characters
  tabWidth: 2,            // Use 2 spaces per tab
  useTabs: false,         // Never use tabs
  arrowParens: 'always'   // Always include parens. Example: (x) => x
};
